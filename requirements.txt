# Core dependency
numpy>=1.20

# Optional: enables ~10-16x faster Gibbs sampling in src/ldpred2.py.
# The code runs in pure NumPy if numba is absent.
# numba>=0.59

# Used by the test suite / simulation harness only.
# pytest

# Optional: realistic LD simulation (coalescent-with-recombination) for
# src/simulate.py --ld-model coalescent. The default ar1 model needs only numpy.
# msprime>=1.3

# Optional: only for the benchmark plot scripts (benchmarks/*.py, incl.
# make_paper_figures.py which builds benchmarks/figures.pdf). Not needed at runtime.
# matplotlib>=3.5
