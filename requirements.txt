# Dependencies are declared authoritatively in pyproject.toml; this file is a
# convenience pointer. Prefer installing the package (which reads pyproject):
#
#   pip install -e .                    # core runtime (numpy only)
#   pip install -e ".[fast]"            # + numba  (JIT sampler; recommended)
#   pip install -e ".[fast,sim,test]"   # + msprime + pytest (development)
#   pip install matplotlib              # only for benchmarks/*.py figure scripts
#
# The one hard runtime dependency:
numpy>=1.20
